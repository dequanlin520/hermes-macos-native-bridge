You are working on the GitHub project `hermes-macos-native-bridge`.

Read these files before making changes:

- AGENTS.md
- README.md
- SECURITY.md
- CONTRIBUTING.md
- Docs/PRODUCT-SCOPE.md

Current issue:

GOV-001 — Establish repository foundation.

This is a repository-governance task. Do not implement product runtime
functionality, SwiftUI screens, Hermes integration, LaunchAgents, XPC, App
Intents, or Shortcuts execution.

Complete the following work:

1. Improve the root AGENTS.md only where required for clarity. Preserve all
   security and product boundaries.

2. Create GitHub issue forms:

   - .github/ISSUE_TEMPLATE/spike.yml
   - .github/ISSUE_TEMPLATE/feature.yml
   - .github/ISSUE_TEMPLATE/bug.yml
   - .github/ISSUE_TEMPLATE/security.yml
   - .github/ISSUE_TEMPLATE/adr.yml
   - .github/ISSUE_TEMPLATE/config.yml

3. Create:

   - .github/pull_request_template.md
   - Docs/ADR/README.md
   - Docs/DEVELOPMENT-WORKFLOW.md

4. Create an idempotent script:

   Scripts/bootstrap-github.sh

5. The script must use GitHub CLI `gh` and support:

   - default dry-run mode;
   - explicit `--apply` mode;
   - creation of type, area, priority, and risk labels;
   - creation of milestones:
     - M0 Project Foundation
     - M1 Technical Validation
   - creation of six spike issues:
     - SPK-01 Validate managed Hermes Gateway
     - SPK-02 Validate LaunchAgent and XPC topology
     - SPK-03 Validate process-group cleanup
     - SPK-04 Validate Shortcuts execution from LaunchAgent
     - SPK-05 Validate file authorization and FSEvents
     - SPK-06 Validate long-running App Intents

6. The script must:

   - be safe to run repeatedly;
   - quote all shell variables;
   - never store or print a GitHub token;
   - use the authenticated `gh` account;
   - fail clearly when prerequisites are unavailable;
   - avoid duplicate labels, milestones and issues;
   - perform no Git commit, push or merge;
   - perform no write in dry-run mode.

7. Run:

   bash -n Scripts/bootstrap-github.sh

8. Inspect all generated YAML for basic structural correctness.

9. Do not:

   - access the network;
   - access files outside this repository;
   - read Keychain;
   - read ~/.hermes;
   - run the generated script with --apply;
   - commit;
   - push;
   - create product code.

10. At the end, report:

   - files created or modified;
   - validation commands and results;
   - GitHub writes that remain intentionally unexecuted;
   - risks or assumptions.

Keep the change focused on GOV-001.
